<%= form_for(@user) do |f| %>

  <div class="form-group">
    <%= f.label :role %>
    <%= f.select(:role, ["admin", "buyer"]) %>
  </div>

   <div class="form-group">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
